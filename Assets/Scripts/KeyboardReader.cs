using UnityEngine;
using UnityEngine.UI;

public class KeyboardReader : MonoBehaviour
{
    // Use this for initialization
    void Start()
    {
        Debug.Log("Keyboard reader running!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
